# Copia este archivo como .env y completa los valores reales
# También puedes colocar un .env en la RAÍZ del repo; si existen ambos,
# Django/.env tiene prioridad (ver WowDash/settings.py)

# Django
DJANGO_SECRET_KEY=pon-una-clave-segura
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*
# Para Codespaces/GitHub u otros dominios agrega tu URL completa (origen):
# CSRF_TRUSTED_ORIGINS=https://<tu-workspace>-8000.app.github.dev,https://localhost:8000

# Base de datos principal (recomendado)
# Formato: postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
DATABASE_URL=postgres://usuario:password@host:5432/base?sslmode=require

# Alternativa si no usas DATABASE_URL
# DB_NAME=
# DB_USER=
# DB_PASSWORD=
# DB_HOST=
# DB_PORT=5432

# Forzar SQLite en desarrollo (ignora DATABASE_URL y DB_*)
# DJANGO_USE_SQLITE=0

# Puerto sugerido para runserver
PORT=8000
